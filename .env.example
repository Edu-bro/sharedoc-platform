# 환경 설정 예시 파일
# 실제 사용시 .env 파일로 복사하여 사용하세요

# JWT 시크릿 키 (프로덕션에서는 반드시 변경)
JWT_SECRET=your-secret-key-change-in-production

# AI 제공자 설정 (mock 또는 deepseek)
AI_PROVIDER=mock
AI_ENABLED=true

# Deepseek API 설정 (AI_PROVIDER=deepseek인 경우)
DEEPSEEK_API_KEY=your-deepseek-api-key

# 서버 포트
PORT=3001

# Node 환경
NODE_ENV=development

# 데이터베이스 설정 (SQLite 파일 경로)
DB_PATH=./database.sqlite

# 업로드 파일 제한 (바이트 단위)
MAX_FILE_SIZE=52428800

# CORS 설정 (프로덕션 도메인)
CORS_ORIGIN=http://localhost:3000